# CLAUDE.md

这是 YOLOps-Agent 的精简维护说明。

目标不是记录所有历史实现细节，而是让后续继续维护、演示、收尾时能快速对齐项目现状。

## 项目定位

- 这是一个面向目标检测流程的轻量化 YOLOps 工作台。
- 当前定位是实习作品 / 单机演示项目，不是生产级多租户系统。
- 优先级是跑通链路、界面可演示、代码结构清楚，而不是追求复杂部署和高并发能力。

## 当前技术栈

- 前端：Vue 3 + Vite
- 后端：FastAPI
- 训练：Ultralytics YOLO
- 数据库：SQLite 默认，PostgreSQL 可切换
- 存储：本地磁盘保存图片、标注、模型和训练产物

## 核心业务链路

项目 -> 数据集 / 标注 -> 训练 -> 模型 -> Agent 分析

当前最适合演示的流程：

1. 创建项目
2. 导入数据集或进入标注页
3. 在全局训练入口或项目内发起训练
4. 在模型页查看归档结果
5. 在 Agent 页做训练 / 模型 / 数据集分析

## 本地启动

前端：

```powershell
cd D:\workspace\YOLOps-Agent\frontend
npm install
npm run dev
```

后端：

```powershell
cd D:\workspace\YOLOps-Agent\backend
python -m venv .venv
.\.venv\Scripts\activate
pip install -r requirements.txt
copy .env.example .env
uvicorn app.main:app --reload --host 127.0.0.1 --port 8009
```

默认地址：

- 前端：`http://127.0.0.1:5174`
- 后端：`http://127.0.0.1:8009`
- API 文档：`http://127.0.0.1:8009/docs`

## 数据库策略

- 本地演示、面试展示、单机交付：优先使用 `SQLite`
- 需要多人共用、独立数据库服务时：切换到 `PostgreSQL`

配置入口见 [backend/.env.example](./backend/.env.example)。

## 仓库边界

仓库只保留源码、必要文档和配置模板，不提交运行产物：

- 不提交 `backend/data/`
- 不提交 `backend/datasets/`
- 不提交训练权重、导出模型、大体积图片
- 不提交 `.env`、`.venv`、`node_modules`

这个项目的真实业务资源主要在磁盘，不应该塞进 Git。

## 当前约束

- 训练和评估仍是后端进程内任务，适合单机，不适合生产调度
- 前端已有多页和较多视图状态，但缓存、预取、统一刷新机制还不完整
- 目前更偏“可演示闭环”，不是“高可靠长期运行系统”
- 数据、模型、项目之间已经能关联，但命名规范和全局一致性仍需要继续收紧

## 已知不足

### 1. 性能和体验

- 某些页面切换、任务创建后刷新仍有白屏等待
- 列表页和概览页的同步更新还不够即时
- 部分页面信息密度高，但层次还可以更清楚

### 2. 工程化

- 自动化测试覆盖不足
- 缺少更正式的数据库迁移方案
- 缺少 CI、打包验证、发布前检查流程

### 3. 架构层面

- 任务系统还不是独立 worker 架构
- Agent 模块可用，但离“稳定产品能力”还有差距
- 权限、审计、多用户隔离不是当前重点，也还没有完整做完

## 后续优化方向

后面如果继续投入时间，建议按下面顺序做。

### 第一优先级：性能和交互一致性

- 梳理前端缓存和刷新策略，减少全页白屏刷新
- 给高频页面做更明确的局部 loading、骨架屏、预取
- 统一概览、项目、训练、模型之间的数据同步逻辑

### 第二优先级：任务系统

- 把训练 / 评估从进程内线程逐步拆到独立 worker
- 明确任务队列、取消、失败重试、资源占用状态

### 第三优先级：数据和模型治理

- 统一项目名、训练名、模型名的命名规则
- 补齐更稳定的模型版本说明、来源追踪、绑定关系展示
- 完善数据集统计、质量检查、训练前校验

### 第四优先级：工程化补强

- 增加关键 API 和核心页面的测试
- 补正式 migration
- 规范 SQLite / PostgreSQL 双模式切换和初始化流程
- 整理最终打包交付链路

## 文档策略

当前文档其实不算多，问题不是“文档太多”，而是“容易重复、过时、分散”。

后续建议保持这套结构：

- `README.md`：给仓库访客看的入口说明
- `CLAUDE.md`：给后续维护时快速对齐现状的内部说明
- `docs/项目实现与架构总览.md`：唯一保留的详细说明文档
- `docs/实习面试项目讲法.md`：给面试讲项目时使用
- `docs/YOLOps-Agent 打包发布实施清单.md`：只有打包时再看

结论：

- 现在不用继续扩写很多新文档
- 以后优先更新 `README.md` 和 `CLAUDE.md`
- 详细实现、接口、模块、架构说明只保留一份总览文档
- 其他文档保持专题化，避免把同一件事写三遍

## 维护原则

- 继续做这个项目时，优先保证演示链路稳定
- 小步修改，不做大范围重构
- 界面优化优先看信息层级、响应速度、状态同步，不要只改颜色
- 新增功能前先判断它是否真的提升演示价值或简历价值

## 相关文档

- [README.md](./README.md)
- [backend/.env.example](./backend/.env.example)
- [docs/项目实现与架构总览.md](./docs/%E9%A1%B9%E7%9B%AE%E5%AE%9E%E7%8E%B0%E4%B8%8E%E6%9E%B6%E6%9E%84%E6%80%BB%E8%A7%88.md)
- [docs/实习面试项目讲法.md](./docs/实习面试项目讲法.md)
- [docs/YOLOps-Agent 打包发布实施清单.md](./docs/YOLOps-Agent%20%E6%89%93%E5%8C%85%E5%8F%91%E5%B8%83%E5%AE%9E%E6%96%BD%E6%B8%85%E5%8D%95.md)
