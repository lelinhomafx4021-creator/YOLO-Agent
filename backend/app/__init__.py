"""
=============================================================================
 YOLOps-Agent 后端应用包 —— __init__.py

【模块用途】
  本文件是 YOLOps-Agent 系统后端 FastAPI 应用的包初始化入口。Python 将
  包含该文件的目录视作一个包（package），因此该文件在包被首次导入时自动执行。

【核心职责】
  1. 定义包的版本号 __version__，供外部（如 setup.py、main.py）统一引用
  2. 以模块级 docstring 的形式承载整个后端应用的用途说明、架构概述
     以及各子模块的功能简述

【所属项目】
  YOLOps-Agent —— 工业级 YOLO 数据集管理、训练归档与 AI 复盘平台

【相关文件】
  - backend/main.py          ：FastAPI 应用实例创建与 uvicorn 启动入口
  - backend/app/routers/     ：各业务子模块的路由定义目录
  - backend/app/core/        ：核心工具与配置模块
  - backend/app/models/      ：数据库模型定义
  - backend/app/schemas/     ：Pydantic 请求/响应模型

【版本历史】
  - v0.1.0 (2025-01) : 初始版本，定义包结构与版本常量
=============================================================================
"""

# ---------------------------------------------------------------------------
# 包版本号
# ---------------------------------------------------------------------------
# 当前版本为 v0.1.0（内部开发预览版），遵循语义化版本规范 MAJOR.MINOR.PATCH。
# 该版本号与 backend/main.py 中 FastAPI 应用实例化时传入的 version 参数保持一致，
# 确保在任意位置引用 __version__ 都能获取到统一的版本信息。
#
# 更新说明：主版本号（MAJOR）在发生不兼容的 API 变更时递增；
#           次版本号（MINOR）在向下兼容的新功能添加时递增；
#           修订号（PATCH）在向下兼容的问题修正时递增。
# ---------------------------------------------------------------------------
__version__ = "0.1.0"
