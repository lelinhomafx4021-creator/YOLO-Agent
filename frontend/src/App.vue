<!-- ============================================================
  App.vue - 应用根组件
  YOLOps-Agent 工程级目标检测平台
  ------------------------------------------------------------
  用途：作为 Vue 应用的入口组件，挂载整个应用的主布局结构
  （AppShell），不处理任何业务逻辑，仅负责渲染外壳。
  ============================================================ -->

<template>
  <!-- 主布局外壳：包含侧边栏导航、顶部栏、工作区路由视图 -->
  <AppShell />
</template>

<script setup>
/**
 * -----------------------------------------------------------
 * 脚本 - 组件逻辑
 * -----------------------------------------------------------
 * 根组件不承载业务逻辑，仅引入并渲染 AppShell 布局组件。
 * 所有导航菜单、页面路由、状态管理均在 AppShell 及其子
 * 组件中完成。
 */

// 主布局外壳组件（侧边栏 + 顶部栏 + 路由视口）
import AppShell from './layouts/AppShell.vue'
</script>
